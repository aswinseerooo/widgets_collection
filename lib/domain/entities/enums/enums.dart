enum HomeTabType { widgets, packages, techniques }

enum TweenType { color, size, alignment }

enum DeliveryType { pickup, delivery, dineIn }

enum FilterType { veg, nonVeg, drinks }

enum Fruit { apple, banana, orange }

enum Transport { car, bike, train }

enum Country {
  india,
  usa,
  germany,
  japan,
  canada,
  australia,
  france,
  brazil,
  italy,
  spain,
  china,
  russia,
  southAfrica,
  egypt,
}
